name: DNS Monitor CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.10"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest requests pycryptodome
        # 如果有 requirements.txt，就改用 pip install -r requirements.txt

    - name: Run Unit Tests with pytest
      run: |
        pytest tests/